interface:
  display_name: "ExercisesEagles Workflow Legacy Router"
  short_description: "Compatibility entrypoint that routes old ExercisesEaglesWorkflow calls to the split project workflow skills."
  default_prompt: "Use $exercises-eagles-legacy-workflow only as a compatibility router, then delegate to the focused exercises-eagles project skill for the current Tixiaoying task."

policy:
  allow_implicit_invocation: true
